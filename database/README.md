# Database for SQL Learning Application

This directory holds all database-related files, including:

- **Schema definitions:** SQL files describing the structure of the database tables.
- **Migration scripts:** Scripts to manage changes to the database schema over time.
- **Seed data:** Initial data to populate the database for development and testing.

## Database System

(Specify the chosen SQL database system, e.g., PostgreSQL, MySQL, SQLite)

## Schema

(Details about the database schema will be documented here or in separate `.sql` files within a `schema/` subdirectory.)

Example:
- `users` table
- `lessons` table
- `exercises` table
- `user_progress` table

## Migrations

(Information on how migrations are handled, e.g., using a tool like Flyway or Knex.js migrations.)

## Seeding

(Instructions on how to populate the database with initial seed data.)
